import SchemeDetailPage from "@/components/SchemeDetailPage";
import { schemeDetails } from "@/data/schemeDetails";

const PMEGP = () => {
  return <SchemeDetailPage scheme={schemeDetails.pmegp} />;
};

export default PMEGP;